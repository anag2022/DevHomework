// Stack

/*using System.Collections;
using System.Linq.Expressions;
using System;

class Program{

    static void Main(string[] args){
   
        Stack stack = new Stack();

        //Push elemtns
        int n = Convert.ToInt16(Console.ReadLine()); // 

        for(int j = 0; j < n; j++){
            
            stack.Push(Console.ReadLine()); //insert values in stack
        }

        Console.WriteLine("count after add: " + stack.Count);

        // Display elements
        foreach(var item in stack){

            Console.WriteLine("stack item: " + item); 
        }

        //Peek
        Console.WriteLine("last item added in stack: " + stack.Peek());
        
        //Pop - remove all items from the stack 
        while(stack.Count > n-1){
            stack.Pop(); 
        }
            Console.WriteLine("count after Pop: " + stack.Count);
        }

        //Console.ReadKey();
        
    }
         */



 
 



